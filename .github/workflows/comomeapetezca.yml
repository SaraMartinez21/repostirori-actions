name: Preparant per producció
run-name: ${{ github.actor }} està pujant l'aplicació a PROD  🚀
on: [push]
jobs:
  Pujar-a-produccio-amb-SCP:
    runs-on: ubuntu-latest
    steps:
      - name: Obtenint el codi del respositori
        uses: actions/checkout@v4

      - name: scp a produccio
        run: |
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/prod_key.pem
          chmod 600 ~/prod_key.pem
          cd ${{ github.workspace }}
          ls -la
          stat ~/prod_key.pem
          ls ./web/
          pwd
          scp -v -r -o stricthostkeychecking=no -o UserKnownHostsFile=/dev/null -i "~/prod_key.pem"  ./test.txt   ${{ secrets.REMOTE_USER }}@${{ secrets.REMOTE_HOST }}:/dev/null

      - run: echo "🍏 This job's status is ${{ job.status }}."